apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm-thirdparty.fullname" . }}-test-connection"
  labels:
{{ include "helm-thirdparty.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "helm-thirdparty.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
