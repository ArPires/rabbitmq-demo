apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-consumer.fullname" . }}
  labels:
{{ include "helm-consumer.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "helm-consumer.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
