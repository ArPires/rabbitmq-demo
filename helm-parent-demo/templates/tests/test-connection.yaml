apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm-parent-demo.fullname" . }}-test-connection"
  labels:
{{ include "helm-parent-demo.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "helm-parent-demo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
