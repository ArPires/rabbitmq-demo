apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm-producer.fullname" . }}-test-connection"
  labels:
{{ include "helm-producer.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "helm-producer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
